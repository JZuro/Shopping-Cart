# Shopping Cart
A single-page e-commerce demo built with React, TypeScript, and Tailwind CSS. Fetches products from the Fake Store API with localStorage caching, lets users browse items with a product detail modal, add/remove from a persistent cart, and includes a responsive navbar with dark/light theme toggle that respects system preference.
